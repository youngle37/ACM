#include <iostream>
#define _ ios_base::sync_with_stdio(false);cin.tie(0);

#define N 100;

using namespace std;

struct Edge{
    int to;
    int w;
    int next;
} edge[N * 2];

int cnt_edge = 0;
int head[N * 2];

void add_edge(int from, int to, int w){
    edge[cnt_edge].to = to;
    edge[cnt_edge].w = w;
    edge[cnt_edge].next 
}

int main(){
    _

    

    return 0;
}
