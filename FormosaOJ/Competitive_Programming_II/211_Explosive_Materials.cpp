#include <iostream>
#include <set>
#include <vector>

#define _ ios_base::sync_with_stdio(false);cin.tie(0);

using namespace std;

int main() {
    _
    int T;
    int n, m;
    cin >> T;
    vector<vector<int>> G(1001, vector<int>());


    while(T--) {
        cin >> n >> m;

    }
    return 0;
}
